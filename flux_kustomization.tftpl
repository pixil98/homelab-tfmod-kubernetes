apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ${name}
  namespace: ${namespace}
spec:
  interval: ${interval}
  sourceRef:
    kind: ${source.kind}
    name: ${source.name}
  path: ${path}
  prune: ${prune}
  timeout: ${timeout}